{
  "name": "@dracing/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "PORT=3001 API_ORIGIN=http://localhost:3001 APP_ORIGIN=http://localhost:5180 DATABASE_URL=postgresql://dracing:dracing@localhost:55432/dracing ADMIN_EMAILS=soporte.francisco.meza@gmail.com tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@date-fns/tz": "^1.5.0",
    "@dracing/contracts": "workspace:*",
    "@dracing/database": "workspace:*",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^11.0.0",
    "fastify": "^5.8.5",
    "openid-client": "^6.8.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@vitest/coverage-v8": "^4.1.9",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
